# Buttondown — mailing list signups (server-side only, used by /api/subscribe)
BUTTONDOWN_API_KEY=

# Sanity — journal content (optional; the site falls back to built-in posts
# until these are set)
SANITY_PROJECT_ID=
SANITY_DATASET=production
